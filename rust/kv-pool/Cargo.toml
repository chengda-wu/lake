[package]
name = "lake-kv-pool"
version = "0.0.0"
edition = "2021"
publish = false

[[bin]]
name = "lake-kv-pool"
path = "src/main.rs"

[dependencies]
lake-proto.workspace = true
tonic.workspace = true
tokio.workspace = true
prost.workspace = true

# P4.4:TcpDataService 内存字节池(TCP 退化数据面;对齐 Mooncake MC_FORCE_TCP)。
