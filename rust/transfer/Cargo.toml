[package]
name = "lake-transfer"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
lake-proto.workspace = true
tonic.workspace = true
tokio.workspace = true
tokio-stream = { workspace = true }
prost.workspace = true

# P4.4: Transport trait + TcpTransport + TransferService gRPC。
# 参考 Mooncake transfer-engine `transport.h` / `TcpTransport`(MC_FORCE_TCP)。
# P5:同 trait 加 RdmaTransport(FFI),业务零改。
