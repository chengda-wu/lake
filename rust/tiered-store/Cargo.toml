[package]
name = "lake-tiered-store"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
lake-proto.workspace = true
tonic.workspace = true

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "tier_access"
harness = false

# P4.3: L0–L3 Memory 站位 + BandwidthPool + TierPipeline + HitStats。
