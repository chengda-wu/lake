[package]
name = "lake-tiered-store"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
lake-proto.workspace = true
tonic.workspace = true

# P4.3: MemoryL2 + LocalTierEngine（promote/demote 最小可用；真 NVMe/L3 后续）。
